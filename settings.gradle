plugins {
    //When your project requires a specific Java version that isn't installed locally,
    //this plugin automatically downloads and provisions the correct JDK.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'fulfilment'
